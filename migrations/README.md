# Root Migrations Placeholder

This directory is currently unused by the Django apps.

Actual Django migrations live inside each app's own `migrations/` directory, such as `accounts/migrations/`, `properties/migrations/`, and `predictions/migrations/`.
