# Predictions Migrations

This directory contains Django database migrations for the `predictions` app.

The initial migration creates the `predictions_prediction` table, including the feature snapshot, predicted price, model name, user foreign key, and optional property foreign key.
