# Database Placeholder

This directory is currently unused.

Database configuration for the active Django app is handled in `realestate_project/settings.py`, and schema changes are managed by Django migrations inside each app.
