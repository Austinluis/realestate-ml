# Source Placeholder

This directory is currently not used by the Django project.

The active application code lives in Django apps at the project root: `accounts/`, `properties/`, `predictions/`, `ml/`, and `realestate_project/`.
