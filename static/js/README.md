# JavaScript

This directory contains shared JavaScript loaded by `templates/base.html`.

`main.js` currently handles the mobile menu toggle and automatic alert dismissal. Page-specific scripts live inside their respective templates when they are tightly coupled to that page.
