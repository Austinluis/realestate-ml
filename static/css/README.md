# CSS

This directory contains the source stylesheet for the EstateIQ interface.

`style.css` defines the shared design system, navbar, cards, forms, tables, property cards, prediction result UI, auth pages, footer, and responsive behavior.
