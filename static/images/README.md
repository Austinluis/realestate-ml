# Static Images

This directory is reserved for source image assets that should ship with the application.

Uploaded property photos are not stored here; they are stored under `media/property_photos/`.
