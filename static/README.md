# Static Assets

This directory contains source static files used by the Django templates.

## Structure

- `css/`: custom application stylesheet.
- `js/`: small shared JavaScript helpers.
- `images/`: reserved for source image assets.

Generated files from `collectstatic` belong in `staticfiles/`, not here.
