# Dashboard Templates

This directory contains the authenticated dashboard template.

`index.html` shows portfolio counts, quick actions, recent properties, and recent prediction records for the logged-in user.
