# Account Templates

This directory contains authentication templates.

- `login.html`: email/password login form.
- `register.html`: new user registration form.

Both templates extend `templates/base.html` and use the shared visual styles.
