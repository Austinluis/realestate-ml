# Prediction Templates

This directory contains templates for price prediction.

- `predict.html`: interactive prediction form that posts feature values with JavaScript and displays the returned price.
- `history.html`: table of previous predictions for the logged-in user.

The prediction form can also prefill fields from a selected property.
