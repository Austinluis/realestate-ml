# Accounts Migrations

This directory contains Django database migrations for the `accounts` app.

The initial migration creates the custom `accounts_user` table used by the project's email-based user model. Do not edit applied migrations manually unless you are intentionally managing a database migration issue.
