# Properties Migrations

This directory contains Django database migrations for the `properties` app.

The migrations create and update the `properties_property` table. Current schema includes property listing data and the optional `photo` upload field.
